apiVersion: solution.sap/v1
kind: Solution

metadata:
  name: ewm-pallet-verification
  version: "1.0.0"
  description: "AI-powered pallet verification agent for SAP EWM outbound loading — detects HU labels via vision AI and cross-references against live EWM delivery data."

assets:
  - ref: ./assets/pallet-verification-agent/asset.yaml
  - ref: ./assets/ewm-outbound-delivery-mcp-server/asset.yaml
  - ref: ./assets/ewm-handling-unit-mcp-server/asset.yaml
